{
  "name": "booth-buddy-app",
  "version": "0.1.0",
  "private": true,
  "description": "BOOTH BUDDY - local backend (Claude API + simple RAG over Markdown) and front-end",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
